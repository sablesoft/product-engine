slug: {{product_slug}}
name: {{product_name}}
profile: embedded
purpose: {{product_purpose}}
scope: {{product_scope}}

distribution:
  model: embedded
  root_skill_folders: []

workspace:
  root: products/{{product_slug}}/workspace
  entities: []

runtime:
  enabled: true
