# Mode Structure Checklist

Before creating a new mode, check:

- Does this belong to an existing product?
- Is this really a mode and not just a product-local skill?
- Does the mode have a clear purpose and boundary?
- Does the mode need dedicated assets yet?
- Should the mode be declared in `product.yaml` now?
- Can the first scaffold stay minimal?
